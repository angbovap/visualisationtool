# Assets

Static images used by the app (logos, icons that aren't inline SVG). Import
them directly in `App.tsx`, e.g. `import vapLogo from '@/assets/vap-logo.png'`
and Vite bundles them automatically.

This file exists only so the empty folder survives in git — delete it once
there's a real asset in here.
